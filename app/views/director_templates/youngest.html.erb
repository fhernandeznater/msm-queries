<h1>Youngest director</h1>

<% youngest_director = Director.where.not({ :dob => nil }).order({ :dob => :desc}).first %>

The youngest director in our dataset right now is <%=youngest_director.name%>, born on <%= youngest_director.dob.strftime("%B %u, %Y")%>.
